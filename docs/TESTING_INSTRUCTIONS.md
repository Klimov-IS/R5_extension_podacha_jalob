# 🧪 Инструкция по Тестированию Backend API Интеграции

**Дата:** 28 января 2026
**Версия:** 1.3.0
**Статус:** Готово к тестированию

---

## ⚠️ ВАЖНО: Перед Тестированием

### 1. Перезагрузите Расширение

**ОБЯЗАТЕЛЬНО!** После обновления `manifest.json` нужно перезагрузить расширение:

```
1. Откройте chrome://extensions/
2. Найдите "Р5 подача жалоб"
3. Нажмите кнопку "Обновить" (круговая стрелка)
4. Или отключите и включите расширение снова
```

**Почему это важно:**
- В `manifest.json` добавлено новое разрешение: `http://158.160.217.236/*`
- Без перезагрузки Chrome не применит новые permissions
- Это была причина ошибки HTTP 503

---

## 📋 План Тестирования

### Этап 1: Тест Настроек (5 минут)

#### Шаг 1.1: Откройте Настройки
```
1. Правый клик на иконке расширения
2. Выберите "Параметры" или "Options"
3. Прокрутите до секции "🔧 Backend API Configuration"
```

#### Шаг 1.2: Введите Тестовые Credentials
```
Backend URL: http://158.160.217.236
Backend Token: d794d4408ef0955a693afaa913dd195ddd25f447e94d098c2f6e0155aa2b0038
Store ID: ss6Y8orHTX6vS7SgJl4k
```

*(Credentials уже предзаполнены в help text - можно скопировать оттуда)*

#### Шаг 1.3: Тест Подключения
```
1. Нажмите "🏥 Проверить подключение к Backend"
2. Ожидаемый результат: ✅ Подключение успешно! Версия: 2.0.0 (Rate: XX/100)
```

**Если ошибка:**
- ❌ "Health check failed: HTTP 503" → Перезагрузите расширение (см. выше)
- ❌ "Ошибка сети или CORS" → Проверьте что `manifest.json` обновлен
- ❌ "Неверный Backend Token" → Проверьте правильность токена
- ❌ "Неверный Store ID" → Проверьте правильность Store ID

#### Шаг 1.4: Сохраните Настройки
```
1. Нажмите "Сохранить настройки" внизу страницы
2. Должно: "Настройки успешно сохранены!"
```

---

### Этап 2: Тест API в Консоли (10 минут)

#### Шаг 2.1: Откройте DevTools Console
```
1. На странице настроек нажмите F12
2. Перейдите на вкладку "Console"
```

#### Шаг 2.2: Проверьте Settings Service
```javascript
// В консоли выполните:
chrome.storage.sync.get('settings', (result) => {
  console.log('Backend Settings:', {
    endpoint: result.settings.backendEndpoint,
    tokenLength: result.settings.backendToken?.length,
    storeId: result.settings.backendStoreId
  });
});
```

**Ожидаемый результат:**
```javascript
Backend Settings: {
  endpoint: "http://158.160.217.236",
  tokenLength: 64,
  storeId: "ss6Y8orHTX6vS7SgJl4k"
}
```

#### Шаг 2.3: Тест Health Endpoint
```javascript
// Прямой запрос к health endpoint
fetch('http://158.160.217.236/api/health')
  .then(r => r.json())
  .then(data => console.log('Health:', data))
  .catch(err => console.error('Error:', err));
```

**Ожидаемый результат:**
```json
{
  "status": "degraded",
  "version": "2.0.0",
  "environment": "production",
  "services": {
    "database": { "status": "healthy" },
    "rateLimiter": { "status": "healthy" }
  }
}
```

**Примечание:** `status: "degraded"` это нормально (cronJobs не инициализированы, но API работает).

#### Шаг 2.4: Тест Complaints Endpoint
```javascript
// Запрос жалоб с авторизацией
fetch('http://158.160.217.236/api/stores/ss6Y8orHTX6vS7SgJl4k/complaints?take=3', {
  headers: {
    'Authorization': 'Bearer d794d4408ef0955a693afaa913dd195ddd25f447e94d098c2f6e0155aa2b0038'
  }
})
  .then(r => r.json())
  .then(complaints => {
    console.log(`Получено жалоб: ${complaints.length}`);
    console.log('Первая жалоба:', complaints[0]);
    console.log('reviewKey format:', complaints[0]?.reviewKey);
  })
  .catch(err => console.error('Error:', err));
```

**Ожидаемый результат:**
```
Получено жалоб: 3
Первая жалоба: {
  id: "Sqe3RgPnbpJMke3xi0bU",
  productId: "391988959",
  rating: 3,
  reviewDate: "2026-01-23T08:38:44.000Z",  // ISO 8601 формат!
  reviewKey: "391988959_3_2026-01-23T08:38:44.000Z",  // Ключ с временем!
  complaintData: { reasonId: "11", ... },
  reviewDateFormatted: "23.01.2026"
}
```

**Проверьте:**
- ✅ `reviewKey` содержит **полный ISO 8601** с временем
- ✅ `complaintData` распарсен автоматически
- ✅ `reviewDateFormatted` в формате DD.MM.YYYY

---

### Этап 3: Тест Content Script на WB (15 минут)

#### Шаг 3.1: Откройте Страницу WB с Отзывами
```
Примеры страниц:
https://seller.wildberries.ru/supplier-feedback/questions-feedbacks/feedbacks
```

#### Шаг 3.2: Откройте Console на Странице WB
```
F12 → Console
```

#### Шаг 3.3: Проверьте DataExtractor
```javascript
// Найдите первую строку отзыва
const table = document.querySelector(".Base-table-body__F-y98zdE6m");
const firstRow = table?.children[0];

if (firstRow) {
  // Извлекаем дату
  const reviewDate = window.DataExtractor.getReviewDate(firstRow);
  console.log('Extracted reviewDate:', reviewDate);
  // Должно: "2026-01-23T08:38:44.000Z" (ISO 8601!)

  // Извлекаем рейтинг
  const rating = window.DataExtractor.getRating(firstRow);
  console.log('Extracted rating:', rating);
  // Должно: число от 1 до 5

  // Создаем ключ
  const key = window.DataExtractor.createReviewKey('391988959', rating, reviewDate);
  console.log('Generated key:', key);
  // Должно: "391988959_3_2026-01-23T08:38:44.000Z"
} else {
  console.error('Таблица отзывов не найдена');
}
```

**Проверьте логи:**
```
[DataExtractor] Extracted datetime text: 14.08.2025 в 15:17
[DataExtractor] Parsed ISO datetime: 2025-08-14T15:17:00.000Z
[DataExtractor] Extracted review data: {
  productId: "391988959",
  rating: 3,
  reviewDate: "2025-08-14T15:17:00.000Z",
  key: "391988959_3_2025-08-14T15:17:00.000Z"
}
```

**Важно:**
- ✅ Дата извлекается в формате `"14.08.2025 в 15:17"` с WB страницы
- ✅ Конвертируется в ISO 8601 `"2025-08-14T15:17:00.000Z"`
- ✅ Ключ содержит **полное время** для уникальности

---

### Этап 4: Интеграционный Тест (20 минут)

#### Шаг 4.1: Полный Цикл Подачи Жалоб
```
1. Откройте Popup расширения
2. Выберите режим "Подача жалоб"
3. Введите артикул товара с отзывами
4. Нажмите "Подать жалобы"
5. Следите за процессом в консоли
```

**Ожидаемое поведение:**
```
1. API запрос: GET /api/stores/ss6Y8orHTX6vS7SgJl4k/complaints
2. Обработка: processComplaints() добавляет reviewKey к каждой жалобе
3. Поиск: SearchService сканирует WB страницу
4. Сопоставление: Находит отзывы по reviewKey (с точностью до секунды!)
5. Подача: ComplaintService подает жалобы
6. Отметка: POST /api/stores/.../reviews/.../complaint/sent
```

**Логи для проверки:**
```
[PilotAPI] ✅ Получено жалоб от API: 10
[PilotAPI] ✅ Жалобы обработаны: {total: 10, withKeys: 10, withParsedData: 10}
[SearchService] Created key: 391988959_3_2026-01-23T08:38:44.000Z
[SearchService] ✅ Найден отзыв: 391988959_3_2026-01-23T08:38:44.000Z
[PilotAPI] 📤 Отправка жалобы на API: {reviewId: "...", duration: 2.5}
[PilotAPI] ✅ Жалоба успешно отправлена
```

---

## 🐛 Troubleshooting - Распространенные Проблемы

### Проблема 1: HTTP 503 при тесте подключения

**Причина:** Расширение не перезагружено после обновления `manifest.json`

**Решение:**
```
1. chrome://extensions/
2. Найдите "Р5 подача жалоб"
3. Нажмите "Обновить" (круговая стрелка)
4. Повторите тест
```

---

### Проблема 2: CORS ошибка

**Причина:** `manifest.json` не содержит разрешение для Backend API

**Проверка:**
```json
// В manifest.json должно быть:
"host_permissions": [
  "*://*.wildberries.ru/*",
  "*://pilot-entry.ru/*",
  "http://158.160.217.236/*"  // ← Это разрешение ОБЯЗАТЕЛЬНО!
]
```

**Решение:**
Если этого разрешения нет - добавьте и перезагрузите расширение.

---

### Проблема 3: reviewKey не создается

**Причина:** DataExtractor не может распарсить дату с WB страницы

**Диагностика:**
```javascript
// Проверьте что WB показывает дату в ожидаемом формате
const dateContainer = document.querySelector('[class*="Col-date-time-with-readmark__date-with-marker"]');
console.log('Date text from WB:', dateContainer?.textContent);
// Должно: "14.08.2025 в 15:17"
```

**Решение:**
Если формат другой - обновите regex в `parseWBDatetime()` функции.

---

### Проблема 4: Отзывы не находятся на странице

**Причина:** Несоответствие ключей между API и страницей

**Диагностика:**
```javascript
// Сравните ключи из API и со страницы
console.log('Key from API:', complaint.reviewKey);
// "391988959_3_2026-01-23T08:38:44.000Z"

console.log('Key from page:', pageReviewKey);
// "391988959_3_2026-01-23T08:38:44.000Z"

// Должны полностью совпадать!
```

**Решение:**
- Проверьте что время точное (до секунды)
- Убедитесь что артикул и рейтинг совпадают
- Проверьте timezone конвертацию (UTC)

---

## ✅ Критерии Успешного Теста

### Must Have (Обязательно):
- ✅ Тест подключения в Настройках успешен
- ✅ API возвращает жалобы с `reviewKey` в ISO 8601 формате
- ✅ DataExtractor извлекает дату И время с WB страницы
- ✅ reviewKey содержит полный timestamp с секундами
- ✅ SearchService находит отзывы по точному ключу
- ✅ Жалобы успешно подаются и отмечаются в Backend

### Nice to Have (Желательно):
- ✅ Rate limit headers видны в логах
- ✅ Обработка идемпотентности (409 статус)
- ✅ Понятные сообщения об ошибках
- ✅ Логирование всех этапов

---

## 📊 Отчет о Тестировании

После тестирования заполните:

```
✅ / ❌ Этап 1: Тест Настроек
✅ / ❌ Этап 2: Тест API в Консоли
✅ / ❌ Этап 3: Тест Content Script
✅ / ❌ Этап 4: Интеграционный Тест

Найденные проблемы:
1. ...
2. ...

Комментарии:
...
```

---

## 🚀 Что Дальше?

После успешного тестирования:

1. **Production Deploy:**
   - Упакуйте расширение (.zip)
   - Опубликуйте обновленную версию
   - Сообщите пользователям об обновлении

2. **Мониторинг:**
   - Следите за ошибками в логах
   - Проверяйте rate limit usage
   - Собирайте feedback от пользователей

3. **Дальнейшие Улучшения:**
   - Добавить API для Verification функции
   - Добавить API для Parsing функции
   - Реализовать multi-store support (Phase 2)

---

**Удачного тестирования! 🎉**
