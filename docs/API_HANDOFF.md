# API Development Handoff - WB Reports Extension

**Дата:** 28 января 2026
**От:** Product Manager, WB Reports Extension Team
**Кому:** Backend Development Team
**Тема:** Разработка нового API сервера (переезд с pilot-entry.ru)

---

## 🎯 Цель проекта

Разработать новый API сервер для Chrome Extension "WB Reports", который автоматизирует подачу жалоб на отзывы в Wildberries.

**Причина переезда:**
- Переезд с pilot-entry.ru на новую инфраструктуру
- Добавление критичного поля `reviewDate` для работы Extension v1.3.0
- Улучшение архитектуры и производительности API

---

## 📦 Что передается

### Документация

1. **API_SPECIFICATION.md** (Полная спецификация)
   - Описание всех endpoints
   - Модели данных
   - Примеры запросов/ответов
   - Требования к безопасности
   - Обработка ошибок
   - **Размер:** ~25 страниц
   - **Язык:** Английский

2. **API_SPECIFICATION_RU_SHORT.md** (Краткая версия на русском)
   - Основные endpoints
   - Критичные требования
   - Чеклист готовности
   - **Размер:** ~8 страниц
   - **Язык:** Русский

3. **API_EXAMPLES.md** (Примеры запросов)
   - cURL команды
   - JavaScript код
   - Postman коллекция
   - Тесты нагрузки
   - **Размер:** ~15 страниц

4. **API_HANDOFF.md** (Этот документ)
   - Краткое резюме для быстрого старта
   - Приоритеты разработки
   - Критичные моменты

### Код Extension (для reference)

- **Репозиторий:** (укажите ваш git URL)
- **Ветка:** `main`
- **Версия:** 1.3.0
- **Ключевые файлы:**
  - `src/api/pilot-api.js` - текущая реализация API клиента
  - `src/background/handlers/complaints-handler.js` - обработчик жалоб
  - `src/contents/complaints/` - модули для работы с WB интерфейсом

---

## ⚡ Quick Start

### Минимально необходимые endpoints (MVP)

Для работы Extension достаточно 4 endpoints:

1. **GET /api/v1/stores**
   - Получить список магазинов пользователя
   - Авторизация: Bearer Token
   - Ответ: массив объектов Store

2. **GET /api/v1/stores/:storeId/complaints**
   - Получить жалобы для обработки
   - Query params: `skip`, `take` (пагинация)
   - **⚠️ КРИТИЧНО:** Поле `reviewDate` обязательно!
   - Ответ: массив объектов Complaint

3. **POST /api/v1/stores/:storeId/reviews/:reviewId/complaint/sent**
   - Отметить жалобу как отправленную
   - Должен быть идемпотентным
   - Ответ: `{success: true, data: {...}}`

4. **GET /api/v1/health**
   - Health check (без авторизации)
   - Ответ: `{status: "ok", version: "1.3.0"}`

### Timeline

| Milestone | Срок | Описание |
|-----------|------|----------|
| **MVP Ready** | 2-3 недели | 4 основных endpoint работают |
| **Testing** | +1 неделя | Integration tests с Extension |
| **Migration** | +1 день | Перенос данных с pilot-entry.ru |
| **Production** | +1 день | Deploy и мониторинг |
| **Total** | ~4-5 недель | От старта до production |

---

## 🔥 Критичные моменты

### 1. Поле reviewDate - ОБЯЗАТЕЛЬНО!

**Формат:** `"DD.MM.YYYY"`

**Примеры:**
- ✅ "18.01.2026"
- ❌ "2026-01-18"
- ❌ "18/01/2026"

**Почему критично:**

Extension v1.3.0 идентифицирует отзывы по ключу:

```
productId + "_" + rating + "_" + reviewDate
Пример: "187489568_1_18.01.2026"
```

**Без этого поля Extension не будет работать!**

Старые данные на pilot-entry.ru НЕ содержат это поле. При миграции нужно:
- Опция A: Спарсить даты с WB
- Опция B: Использовать `createdAt` как fallback
- Опция C: Пометить старые записи как "legacy"

---

### 2. Идемпотентность POST /complaint/sent

**Требование:** Повторные вызовы с тем же `reviewId` должны возвращать `200 OK`, а не `409 Conflict`.

**Причина:** Extension может отправить запрос повторно при сетевых ошибках.

**Правильная реализация:**

```python
def mark_complaint_as_sent(store_id, review_id):
    complaint = db.get_complaint(store_id, review_id)

    if not complaint:
        return 404  # Not Found

    if complaint.status == "sent":
        # Уже отправлена - возвращаем успех (идемпотентность)
        return {
            "success": True,
            "message": "Complaint already marked as sent",
            "data": {"reviewId": review_id, "status": "sent"}
        }

    # Отмечаем как отправленную
    complaint.status = "sent"
    complaint.updated_at = now()
    db.save(complaint)

    return {
        "success": True,
        "message": "Complaint marked as sent",
        "data": {"reviewId": review_id, "status": "sent"}
    }
```

---

### 3. Формат complaintText

**Формат:** JSON обернутый в markdown code block

**Пример:**
```
```json
{"reasonId":"1","reasonName":"Оскорбление","complaintText":"Текст жалобы"}
```
```

**Почему так странно?**

Историческое решение. Первая версия копировала JSON из GPT, который возвращал в markdown. Для обратной совместимости формат сохранили.

**Парсинг в Extension:**

```javascript
const rawJson = complaintText.replace(/```json|```/g, '').trim();
const parsed = JSON.parse(rawJson);
// {reasonId: "1", reasonName: "Оскорбление", complaintText: "..."}
```

**Генерация при создании жалобы:**

```python
import json

complaint_data = {
    "reasonId": "1",
    "reasonName": "Оскорбление",
    "complaintText": "Текст жалобы"
}

complaint_text = "```json\n" + json.dumps(complaint_data, ensure_ascii=False) + "\n```"
```

---

### 4. Rate Limiting

**Требование:** 100 запросов в минуту на токен

**Реализация:**

- Используйте Redis для подсчета
- Ключ: `rate_limit:{token}:{minute}`
- TTL: 60 секунд

**Headers при превышении:**

```
HTTP/1.1 429 Too Many Requests
X-RateLimit-Limit: 100
X-RateLimit-Remaining: 0
X-RateLimit-Reset: 1706461800
Retry-After: 60
```

**Body:**

```json
{
  "error": "Too Many Requests",
  "message": "Rate limit exceeded. Retry after 60 seconds",
  "code": "RATE_LIMIT",
  "retryAfter": 60
}
```

---

### 5. CORS Headers

**Требование:** Extension должен иметь доступ к API

**Headers:**

```
Access-Control-Allow-Origin: chrome-extension://*
Access-Control-Allow-Methods: GET, POST, OPTIONS
Access-Control-Allow-Headers: Authorization, Content-Type
Access-Control-Max-Age: 86400
```

**Альтернатива:** Если не работает `chrome-extension://*`, используйте `*` (но менее безопасно)

---

## 📊 Модели данных (кратко)

### Store

```json
{
  "id": "store_abc123",
  "name": "Магазин Одежды",
  "supplierName": "ООО Поставщик",
  "inn": "1234567890",
  "isActive": true,
  "createdAt": "2025-01-15T10:30:00Z",
  "updatedAt": "2026-01-28T15:45:00Z"
}
```

### Complaint

```json
{
  "id": "review_001",
  "productId": "187489568",
  "rating": 1,
  "reviewDate": "18.01.2026",
  "reviewText": "Ужасное качество",
  "authorName": "Анна К.",
  "complaintText": "```json\n{...}\n```",
  "status": "pending",
  "attempts": 0,
  "createdAt": "2026-01-18T14:25:00Z"
}
```

**Обязательные поля для Extension:**
- ✅ `id`
- ✅ `productId`
- ✅ `rating`
- ✅ `reviewDate` ← **КРИТИЧНО!**
- ✅ `complaintText`

---

## 🔒 Безопасность

### Авторизация

**Формат токена:** `wbrm_` + random string (мин 32 символа)

**Пример:** `wbrm_u1512gxsgp1nt1n31fmsj1d31o51jue`

**Header:**
```
Authorization: Bearer wbrm_u1512gxsgp1nt1n31fmsj1d31o51jue
```

**Хранение:**
- Токены в БД должны быть захешированы (bcrypt/argon2)
- Extension хранит токен в `chrome.storage.sync` (зашифровано Chrome)

### HTTPS Only

- Все запросы только через HTTPS
- HTTP → HTTPS redirect обязателен

### Input Validation

- Валидация всех входных данных
- Санитизация строк (защита от SQL injection, XSS)
- Проверка длины полей (макс 1000 символов для `complaintText`)

---

## 📈 Требования к производительности

### Response Time (95th percentile)

| Endpoint | Target | Critical |
|----------|--------|----------|
| /stores | < 500ms | < 1s |
| /complaints | < 1000ms | < 2s |
| /complaint/sent | < 300ms | < 1s |
| /health | < 100ms | < 200ms |

### Uptime

- **Target:** 99.5% (3.6 часа downtime/месяц)
- **Critical:** 99% (7.2 часа downtime/месяц)

### Пагинация

- Default: 100 записей
- Maximum: 200 записей
- Если запрашивают больше 200 → возвращать ошибку 400

---

## 🧪 Тестирование

### Обязательные тесты

1. **Unit Tests**
   - Каждый endpoint
   - Модели данных
   - Бизнес-логика

2. **Integration Tests**
   - Полный flow: получить жалобы → отметить как отправленные
   - Авторизация
   - Обработка ошибок

3. **Load Tests**
   - 100 RPS в течение 5 минут
   - 1000 одновременных подключений
   - Нет таймаутов, нет ошибок 500

### Test Cases (обязательные)

См. `API_SPECIFICATION.md`, раздел 14 - контрольные примеры.

**Критичные сценарии:**

1. ✅ Получение жалоб с `reviewDate` в правильном формате
2. ✅ Идемпотентность POST /complaint/sent
3. ✅ Rate limiting работает (429 после 100 запросов)
4. ✅ CORS headers корректные
5. ✅ Health check без авторизации

---

## 📚 Документация для разработки

### OpenAPI Spec

Создайте OpenAPI 3.0 спецификацию на основе `API_SPECIFICATION.md`.

**Рекомендуемые инструменты:**
- Swagger Editor
- Stoplight Studio
- Postman (генерация из коллекции)

### Postman Collection

Создайте Postman коллекцию со всеми endpoints и примерами.

**Структура:**

```
WB Reports API v1
├── Health Check
├── Stores
│   └── Get Stores
├── Complaints
│   ├── Get Complaints (first page)
│   ├── Get Complaints (pagination)
│   └── Mark As Sent
└── Tests
    ├── Invalid Token (401)
    ├── Not Found (404)
    └── Rate Limit (429)
```

### Changelog

Ведите changelog в формате:

```markdown
# Changelog

## [1.0.0] - 2026-02-15

### Added
- GET /api/v1/stores
- GET /api/v1/stores/:storeId/complaints
- POST /api/v1/stores/:storeId/reviews/:reviewId/complaint/sent
- GET /api/v1/health
- Bearer Token authentication
- Rate limiting (100 req/min)

### Changed
- Добавлено поле `reviewDate` в Complaint model

### Security
- HTTPS only
- CORS headers для chrome-extension://*
```

---

## 🚀 Приоритеты разработки

### Phase 1: MVP (2-3 недели)

**Цель:** Базовый функционал работает

1. ✅ Database schema
2. ✅ Authentication (Bearer Token)
3. ✅ GET /stores
4. ✅ GET /stores/:storeId/complaints
5. ✅ POST /stores/:storeId/reviews/:reviewId/complaint/sent
6. ✅ GET /health
7. ✅ Error handling
8. ✅ Basic logging

**Критерий готовности:** Extension может получить жалобы и отметить их как отправленные

---

### Phase 2: Production Ready (1 неделя)

**Цель:** Готов к production deploy

1. ✅ Rate limiting
2. ✅ CORS headers
3. ✅ Input validation
4. ✅ Unit tests (>80% coverage)
5. ✅ Integration tests
6. ✅ Load tests (100 RPS)
7. ✅ Monitoring и alerting
8. ✅ Documentation (OpenAPI, Postman)

**Критерий готовности:** Все тесты проходят, документация готова

---

### Phase 3: Migration (1-2 дня)

**Цель:** Перенос данных с pilot-entry.ru

1. ✅ Export данных с pilot-entry.ru
2. ✅ Добавление поля `reviewDate` (парсинг или fallback)
3. ✅ Import в новую БД
4. ✅ Валидация целостности данных
5. ✅ Staging тесты с Extension

**Критерий готовности:** Extension работает с новым API на staging

---

### Phase 4: Production Deploy (1 день)

**Цель:** Production запуск

1. ✅ Production deploy
2. ✅ Обновление DNS (если нужно)
3. ✅ Обновление Extension настроек (новый endpoint)
4. ✅ Мониторинг первых 24 часов
5. ✅ Rollback план (если что-то пошло не так)

**Критерий готовности:** Extension работает в production, нет critical errors

---

## 🛠️ Рекомендуемый стек

**Backend Framework:**
- Node.js: Express / Fastify / NestJS
- Python: FastAPI / Django / Flask
- Go: Gin / Echo
- Java: Spring Boot

**Database:**
- PostgreSQL (рекомендуется)
- MySQL
- MongoDB (если предпочитаете NoSQL)

**Cache:**
- Redis (обязательно для rate limiting)

**Мониторинг:**
- Prometheus + Grafana
- DataDog
- New Relic
- Sentry (для ошибок)

**Deployment:**
- Docker + Kubernetes
- AWS ECS / EKS
- Google Cloud Run
- Heroku (для прототипа)

---

## 📞 Контакты

### Extension Team

**Product Manager:**
- Email: pm@company.com
- Telegram: @pm_username

**Tech Lead:**
- Email: tech@company.com
- Telegram: @tech_username

### Вопросы по API

**Канал для вопросов:**
- Slack: #api-development
- Email: api-questions@company.com

**Issue Tracker:**
- GitHub Issues (приватный репозиторий)
- Jira (если используете)

---

## 📋 Чеклист перед стартом

Убедитесь что у вас есть доступ к:

- [ ] Документация API (API_SPECIFICATION.md)
- [ ] Примеры запросов (API_EXAMPLES.md)
- [ ] Репозиторий Extension (для reference)
- [ ] Доступ к pilot-entry.ru (для экспорта данных)
- [ ] Тестовые токены Extension
- [ ] Staging окружение для тестов
- [ ] Production credentials (БД, DNS и т.д.)

---

## 🎯 Success Criteria

API считается готовым когда:

1. ✅ Все 4 обязательных endpoint работают
2. ✅ Поле `reviewDate` присутствует в Complaint
3. ✅ Extension успешно интегрируется (staging тесты прошли)
4. ✅ Load tests прошли (100 RPS без ошибок)
5. ✅ Документация готова (OpenAPI + Postman)
6. ✅ Мониторинг настроен
7. ✅ Production deploy прошел успешно
8. ✅ Extension работает в production без critical errors 24+ часов

---

## 🚨 Риски и митигация

### Риск 1: Поле reviewDate отсутствует в старых данных

**Вероятность:** Высокая
**Влияние:** Critical

**Митигация:**
- Опция A: Спарсить даты с WB перед миграцией
- Опция B: Использовать `createdAt` как fallback (неточно, но работает)
- Опция C: Старые жалобы (без даты) не обрабатывать

**Решение принимает:** Product Manager + Backend Team

---

### Риск 2: WB изменит интерфейс и Extension сломается

**Вероятность:** Средняя
**Влияние:** High

**Митигация:**
- Extension использует fallback механизмы для поиска элементов
- Monitoring алертов при росте ошибок
- Быстрый rollback на предыдущую версию Extension

**Действие:** Extension Team мониторит WB на изменения

---

### Риск 3: Performance проблемы при высокой нагрузке

**Вероятность:** Средняя
**Влияние:** Medium

**Митигация:**
- Load testing перед production deploy
- Auto-scaling (если используете Kubernetes)
- Caching часто запрашиваемых данных (stores)
- Database indexes на ключевые поля

**Действие:** Backend Team проводит load tests

---

### Риск 4: Downtime при миграции

**Вероятность:** Низкая
**Влияние:** High

**Митигация:**
- Blue-green deployment
- Rollback план (откат на pilot-entry.ru)
- Миграция в низконагруженное время (ночь/выходные)

**Действие:** DevOps готовит rollback скрипты

---

## 📖 Дополнительные ресурсы

### Для изучения Extension

1. **Установка Extension:**
   - Клонировать репозиторий
   - `chrome://extensions/` → Load unpacked
   - Открыть DevTools Console для логов

2. **Настройка Extension:**
   - Правый клик на иконку → Options
   - Ввести тестовый токен
   - Ввести staging endpoint

3. **Тестирование:**
   - Открыть страницу WB отзывов
   - Запустить Extension
   - Проверить логи в Console

### Полезные ссылки

- [Chrome Extension Docs](https://developer.chrome.com/docs/extensions/)
- [Wildberries Seller](https://seller.wildberries.ru/) - целевой сайт
- [OpenAPI Specification](https://spec.openapis.org/oas/v3.0.0)
- [REST API Best Practices](https://restfulapi.net/)

---

## ✅ Next Steps

1. **Backend Team:**
   - Прочитать API_SPECIFICATION.md
   - Задать вопросы в Slack #api-development
   - Создать план разработки
   - Начать Phase 1 (MVP)

2. **Extension Team:**
   - Предоставить доступ к репозиторию
   - Выделить тестовый staging environment
   - Быть доступными для вопросов

3. **DevOps:**
   - Подготовить staging/production окружения
   - Настроить мониторинг и alerting
   - Подготовить rollback план

4. **QA:**
   - Ознакомиться с API_SPECIFICATION.md
   - Подготовить тест-кейсы
   - Настроить автоматизированное тестирование

---

## 🎉 Заключение

Спасибо за работу над проектом!

Этот API - критически важная часть инфраструктуры WB Reports Extension, который используется для автоматизации работы с отзывами на Wildberries.

**Ключевое:**
- ⚠️ Поле `reviewDate` обязательно (формат "DD.MM.YYYY")
- ⚠️ Идемпотентность POST /complaint/sent
- ⚠️ Response time < 1s (95th percentile)

**Если возникнут вопросы - пишите в Slack #api-development!**

Удачи! 🚀

---

**Дата:** 28 января 2026
**Версия документа:** 1.0
**Статус:** Ready for Development ✅
