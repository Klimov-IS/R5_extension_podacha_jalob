# Статусы отзывов Wildberries - Полная документация

## Обзор

Документ содержит полную категоризацию статусов, которые встречаются в системе отзывов Wildberries.
Данные собраны путем парсинга реальных страниц с отзывами (выборка: 2000 отзывов из двух тестовых запусков).

---

## 1. Review Statuses (Статусы отзывов - видимость)

Эти статусы определяют, виден ли отзыв покупателям и участвует ли он в рейтинге товара.

### ✅ "Виден"
- **Описание**: Отзыв опубликован и виден всем покупателям
- **Частота**: 93.4% / 92.7% отзывов
- **Значение для продавца**: Отзыв активно влияет на рейтинг товара
- **Действия**: Можно подать жалобу, если отзыв нарушает правила WB

### ❌ "Снят с публикации"
- **Описание**: Отзыв был снят с публикации модерацией WB
- **Частота**: 6.6% / 7.3% отзывов
- **Значение для продавца**: Отзыв не виден покупателям, не влияет на рейтинг
- **Причины**: Нарушение правил WB, одобренная жалоба продавца

### ⚠️ "Исключён из рейтинга"
- **Описание**: Отзыв виден покупателям, но не учитывается в рейтинге товара
- **Частота**: 3.4% / 9.7% отзывов
- **Значение для продавца**: Отзыв виден, но не влияет на звездный рейтинг
- **Причины**: Частичное нарушение правил, сомнительный отзыв

### 🔒 "Временно скрыт"
- **Описание**: Отзыв временно недоступен для просмотра
- **Частота**: Редко встречается
- **Значение для продавца**: Отзыв на проверке или временно заблокирован
- **Действия**: Ожидать результатов проверки

---

## 2. Complaint Statuses (Статусы жалоб)

Эти статусы показывают результат рассмотрения жалобы продавца на отзыв.

### ❌ "Жалоба отклонена"
- **Описание**: WB отклонил жалобу продавца, отзыв остается опубликованным
- **Частота**: 91.6% / 6.5% отзывов
- **Значение для продавца**: Жалоба не была удовлетворена
- **Действия**: Можно подать повторную жалобу в техподдержку ("Жалоба пересмотрена")

### ✅ "Жалоба одобрена"
- **Описание**: WB одобрил жалобу продавца, отзыв снят с публикации
- **Частота**: 6.6% / 7.3% отзывов
- **Значение для продавца**: Успешное удаление негативного отзыва
- **Результат**: Отзыв получает статус "Снят с публикации"

### 🔄 "Проверяем жалобу"
- **Описание**: Жалоба продавца находится на рассмотрении у модерации WB
- **Частота**: 0.1% / 0.5% отзывов (очень редко)
- **Значение для продавца**: Ожидание решения модерации
- **Время ожидания**: Обычно 24-48 часов

### 📞 "Жалоба пересмотрена"
- **Описание**: Жалоба была подана повторно в техническую поддержку WB
- **Частота**: Данных пока нет (не встречалась в тестовых выборках)
- **Значение для продавца**: Повторное рассмотрение отклоненной жалобы
- **Условие**: Доступно только после получения статуса "Жалоба отклонена"

---

## 3. Product/Order Statuses (Статусы товара)

Эти статусы отражают фактический статус заказа, по которому был оставлен отзыв.

### ✅ "Выкуп"
- **Описание**: Товар был выкуплен покупателем (доставлен и оплачен)
- **Частота**: 16.4% / 95.7% отзывов
- **Значение для продавца**: Подтвержденная покупка
- **Важно**: Отзывы с этим статусом считаются наиболее достоверными

### ❌ "Отказ"
- **Описание**: Покупатель отказался от товара при получении
- **Частота**: 30.3% / 3.5% отзывов
- **Значение для продавца**: Товар не был выкуплен
- **Важно**: Отзывы после отказа могут быть менее релевантными

### 🔄 "Возврат"
- **Описание**: Товар был возвращен покупателем после выкупа
- **Частота**: 3.8% / 0.4% отзывов
- **Значение для продавца**: Покупатель остался недоволен товаром
- **Важно**: Часто сопровождается негативными отзывами

### 🔄 "Запрошен возврат"
- **Описание**: Покупатель инициировал процедуру возврата товара
- **Частота**: 0.2% / 0% отзывов (очень редко)
- **Значение для продавца**: Возврат в процессе оформления
- **Важно**: Промежуточный статус перед "Возврат"

---

## Статистика из реальных данных

### Тест 1 (Артикул #1, 1000 отзывов, 10 страниц)
```
Виден:                  934 (93.4%)
Жалоба отклонена:       916 (91.6%)
Отказ:                  303 (30.3%)
Выкуп:                  164 (16.4%)
Снят с публикации:       66 (6.6%)
Жалоба одобрена:         66 (6.6%)
Возврат:                 38 (3.8%)
Исключён из рейтинга:    34 (3.4%)
Запрошен возврат:         2 (0.2%)
Проверяем жалобу:         1 (0.1%)
```

### Тест 2 (Артикул #2, 1000 отзывов, 10 страниц)
```
Выкуп:                  957 (95.7%)
Виден:                  927 (92.7%)
Исключён из рейтинга:    97 (9.7%)
Снят с публикации:       73 (7.3%)
Жалоба одобрена:         73 (7.3%)
Жалоба отклонена:        65 (6.5%)
Отказ:                   35 (3.5%)
Проверяем жалобу:         5 (0.5%)
Возврат:                  4 (0.4%)
```

---

## Комбинации статусов

Отзыв может иметь **несколько статусов одновременно** (по одному из каждой категории):

### Типичные комбинации:

1. **"Виден" + "Жалоба отклонена" + "Выкуп"**
   - Самая частая комбинация (~85% отзывов)
   - Нормальный выкупленный товар с видимым отзывом

2. **"Снят с публикации" + "Жалоба одобрена" + "Выкуп"**
   - Успешная жалоба продавца
   - Отзыв снят с публикации после одобрения жалобы

3. **"Виден" + "Жалоба отклонена" + "Отказ"**
   - Отзыв после отказа от товара
   - Жалоба продавца не удовлетворена

4. **"Исключён из рейтинга" + "Жалоба одобрена" + "Возврат"**
   - Частично удовлетворенная жалоба
   - Отзыв виден, но не влияет на рейтинг

---

## Использование в коде

### Категории статусов (для маппинга):

```javascript
const STATUS_CATEGORIES = {
  REVIEW: 'review',        // Статусы видимости отзыва
  COMPLAINT: 'complaint',  // Статусы жалобы
  PRODUCT: 'product'       // Статусы товара/заказа
};

const STATUS_MAPPING = {
  // Review statuses
  'Виден': { category: 'review', type: 'visible', action: 'can_complaint' },
  'Снят с публикации': { category: 'review', type: 'hidden', action: 'success' },
  'Исключён из рейтинга': { category: 'review', type: 'excluded', action: 'partial_success' },
  'Временно скрыт': { category: 'review', type: 'temporarily_hidden', action: 'pending' },

  // Complaint statuses
  'Жалоба отклонена': { category: 'complaint', type: 'rejected', action: 'can_resubmit' },
  'Жалоба одобрена': { category: 'complaint', type: 'approved', action: 'success' },
  'Проверяем жалобу': { category: 'complaint', type: 'pending', action: 'wait' },
  'Жалоба пересмотрена': { category: 'complaint', type: 'reconsidered', action: 'wait' },

  // Product statuses
  'Выкуп': { category: 'product', type: 'purchased', action: 'info' },
  'Отказ': { category: 'product', type: 'cancelled', action: 'info' },
  'Возврат': { category: 'product', type: 'returned', action: 'info' },
  'Запрошен возврат': { category: 'product', type: 'return_requested', action: 'info' }
};
```

---

## Важные выводы

1. **Большинство отзывов видны** (92-93%) - основная задача продавца работать с видимыми отзывами
2. **Высокий процент отклоненных жалоб** (6.5-91.6%) - WB строго проверяет обоснованность жалоб
3. **Разброс по статусам товара** - в зависимости от категории товара может сильно различаться
4. **Редкие статусы** - "Проверяем жалобу" и "Запрошен возврат" встречаются менее 1% случаев

---

## Правила подачи жалоб

> **Обновлено:** 01.02.2026
> **Источник:** Консольный тест 15 страниц отзывов

### Статус "Виден" НЕ является Chips-элементом

**Важное открытие:** Статус "Виден" НЕ отображается как отдельный Chips в DOM.
Он **подразумевается по умолчанию** - если у отзыва нет статуса "Снят с публикации",
значит отзыв виден покупателям.

### Когда НЕЛЬЗЯ подать жалобу

Жалобу **НЕЛЬЗЯ** подать, если у отзыва есть **ЛЮБОЙ** из следующих статусов:

| Статус | Причина |
|--------|---------|
| `Жалоба отклонена` | Жалоба уже была подана и отклонена |
| `Жалоба одобрена` | Жалоба уже была подана и одобрена |
| `Проверяем жалобу` | Жалоба на рассмотрении |
| `Жалоба пересмотрена` | Повторная жалоба уже подана |

### Когда МОЖНО подать жалобу

Жалобу **МОЖНО** подать, если у отзыва **НЕТ** ни одного из статусов жалобы выше.

**Примеры:**
- `['Выкуп']` → ✅ Можно подать (нет статуса жалобы)
- `['Отказ', 'Выкуп']` → ✅ Можно подать (нет статуса жалобы)
- `['Выкуп', 'Жалоба отклонена']` → ❌ Нельзя (есть статус жалобы)
- `['Возврат', 'Жалоба одобрена', 'Снят с публикации']` → ❌ Нельзя (есть статус жалобы)

### Код проверки

```javascript
/**
 * Статусы жалоб - если есть любой из них, жалобу подать нельзя
 */
const COMPLAINT_STATUSES = [
  'Жалоба отклонена',
  'Жалоба одобрена',
  'Проверяем жалобу',
  'Жалоба пересмотрена'
];

/**
 * Проверить, можно ли подать жалобу на отзыв
 * @param {string[]} statuses - массив статусов отзыва
 * @returns {boolean} - true если жалобу можно подать
 */
function canSubmitComplaint(statuses) {
  const hasComplaintStatus = statuses.some(s => COMPLAINT_STATUSES.includes(s));
  return !hasComplaintStatus;
}
```

### Статистика из реальных данных (15 страниц)

```
Жалоба отклонена: 1382 (большинство отзывов!)
Жалоба одобрена: 103
Проверяем жалобу: 1
```

**Вывод:** ~80% отзывов уже имеют статус "Жалоба отклонена", поэтому повторную жалобу
на них подать нельзя через стандартный интерфейс.

---

## Изменения интерфейса Wildberries (Январь 2026)

### Обзор изменений

В январе 2026 года Wildberries Seller Portal обновил интерфейс страницы отзывов. Изменения затронули структуру таблицы, визуальное представление данных и расположение элементов.

### 🔴 Критические изменения

#### 1. Новая колонка "Информация о товаре"

**Что изменилось:**
- Добавлена новая колонка в начало таблицы с информацией о товаре
- Содержит: изображение товара, название, категорию, бренд, артикул продавца, артикул WB
- Класс контейнера: `Extended-article-info-card__*`

**Влияние на расширение:**
- Кнопка меню (три точки) больше НЕ находится в последней ячейке строки
- Необходимо обновить селекторы для поиска кнопки

**Решение:**
```javascript
// Старый код (НЕ РАБОТАЕТ):
const cell = row.lastElementChild;

// Новый код (РАБОТАЕТ):
const cells = Array.from(row.children);
for (let i = cells.length - 1; i >= 0; i--) {
  // Проверяем каждую ячейку...
}
```

#### 2. Изменение структуры рейтинга

**Старый интерфейс:**
- Рейтинг обёрнут в несколько div-контейнеров
- Активные звёзды: `Rating--active__*`
- Неактивные звёзды: `Rating--not-active__*`

**Новый интерфейс:**
- Прямые SVG без обёрток
- Активные звёзды: `fill="#FF773C"`
- Неактивные звёзды: `fill="#d1cfd7"`

**Код для определения рейтинга:**
```javascript
// Новый способ:
const stars = row.querySelectorAll('svg path');
const rating = stars.filter(s => s.getAttribute('fill') === '#FF773C').length;
```

#### 3. Разделение даты и времени

**Старый интерфейс:**
- Дата и время в одном элементе: "18.01.2026 в 12:33"
- Класс: `Col-date-time-with-readmark__*`

**Новый интерфейс:**
- Дата: отдельный элемент "18.01.2026"
- Время: отдельный элемент "12:33"
- Оба элемента внутри `Col-date-time-with-readmark__*`

**Код для парсинга:**
```javascript
const dateContainer = row.querySelector('[class*="Col-date-time-with-readmark"]');
const dateText = dateContainer.querySelector('span:first-child').innerText; // "18.01.2026"
const timeText = dateContainer.querySelector('span:last-child').innerText;  // "12:33"
const fullDate = `${dateText} в ${timeText}`;
```

#### 4. Изменение текста "Минусы" → "Недостатки"

**Старый интерфейс:**
```html
<span>Минусы: </span><span>За ночь сдулся</span>
```

**Новый интерфейс:**
```html
<span>Недостатки: </span><span>За ночь сдулся</span>
```

**Код для совместимости:**
```javascript
const consText = feedbackBlock.innerText;
const hasMinusy = consText.includes('Минусы:') || consText.includes('Недостатки:');
```

### Изменения хешей CSS-классов

Все хеши CSS-классов изменились. Примеры:

| Элемент | Старый класс | Новый класс |
|---------|--------------|-------------|
| Кнопка | `button__nqovH0JRnr` | `button__HSQby8Ur+U` |
| onlyIcon | `onlyIcon__xxd+z67tF2` | `onlyIcon__7-9JhVad6J` |
| Chips | `Chips__PibbXEzFHF` | `Chips__4-vBBaBXvo` |
| Base-table-cell | `Base-table-cell__9Nw1bX-CjJ` | `Base-table-cell__9Nw1bX-CjJ` (не изменился) |

**Важно:** Расширение использует **частичное совпадение классов** (`[class*="onlyIcon"]`), поэтому изменения хешей не влияют на работу.

### Стратегия совместимости

Расширение поддерживает оба интерфейса благодаря:

1. **Универсальным селекторам с частичным совпадением**
   ```javascript
   element.querySelector('[class*="Buttons-cell"]')
   ```

2. **Множественным fallback-стратегиям**
   ```javascript
   // Способ 1: По классу
   // Способ 2: По SVG viewBox
   // Способ 3: По path данным
   // Способ 4: По тексту
   ```

3. **Итерации вместо фиксированных индексов**
   ```javascript
   // НЕТ: row.lastElementChild
   // ДА: Array.from(row.children).forEach(...)
   ```

4. **Поиск по характерным атрибутам SVG**
   ```javascript
   const viewBox = svg.getAttribute('viewBox');
   if (viewBox && viewBox.includes('-10') && viewBox.includes('24')) {
     // Это кнопка "три точки"
   }
   ```

### Тестирование

При тестировании расширения необходимо проверить:

- ✅ Работа на старом интерфейсе (обратная совместимость)
- ✅ Работа на новом интерфейсе (основное тестирование)
- ✅ Корректное определение кнопки меню
- ✅ Парсинг рейтинга
- ✅ Парсинг даты и времени
- ✅ Определение статусов

### Документация

Подробная документация по HTML структурам находится в папке `docs/HTML/`:
- `README.md` - полное описание изменений и стратегий совместимости
- Файлы без префикса "2" - старый интерфейс
- Файлы с префиксом "2" - новый интерфейс

---

Документ обновлен: 2026-01-28
Основан на реальных данных парсинга 2000+ отзывов
Обновлён с учётом изменений интерфейса WB (январь 2026)
