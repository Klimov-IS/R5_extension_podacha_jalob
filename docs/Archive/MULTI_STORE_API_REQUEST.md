# 📋 Запрос на Добавление Multi-Store API Endpoint

**Дата:** 28 января 2026
**Версия Backend:** 2.0.0
**Приоритет:** High
**Запрашивает:** Frontend Team (Chrome Extension)

---

## 🎯 Проблема

Текущая ситуация:
- **Backend Token привязан к ОДНОМУ магазину** (Store ID)
- **У пользователя десятки магазинов** для работы
- **При переключении между магазинами** нужно вручную вводить новый Store ID в настройках
- **Это неудобно!** "Головная боль" — слова пользователя

### Текущий Flow (❌ Неудобный):
```
1. Пользователь работает с Магазином A
2. Хочет переключиться на Магазин B
3. Идет в Настройки расширения (options.html)
4. Вручную вводит Store ID для Магазина B
5. Сохраняет настройки
6. Возвращается в complaints-page.html
7. Начинает работу с Магазином B
```

### Желаемый Flow (✅ Удобный):
```
1. Открывает complaints-page.html
2. Видит dropdown со списком магазинов (по названиям!)
3. Выбирает "20Grace ИП Ширазданова Г. М."
4. Расширение автоматически использует правильный Store ID
5. Пользователь НЕ ЗНАЕТ про Store ID вообще
```

**Цитата пользователя:**
> "пользователь как будто вообще и не знает про вот эти ID магазинов"

---

## 🚀 Предлагаемое Решение

### Новый Endpoint: `GET /api/stores`

**Назначение:** Возвращать список всех магазинов, к которым у текущего токена есть доступ.

---

## 📡 API Спецификация

### Endpoint Details

```
GET /api/stores
```

### Request Headers

```http
Authorization: Bearer {user_token}
Content-Type: application/json
```

### Response (Success - 200 OK)

```json
[
  {
    "id": "ss6Y8orHTX6vS7SgJl4k",
    "name": "20Grace ИП Ширазданова Г. М.",
    "isActive": true
  },
  {
    "id": "aB3Xr9qP2tKwL5mN8vZj",
    "name": "Магазин 2 ООО Рога и Копыта",
    "isActive": true
  },
  {
    "id": "mN8vZj2tKwL5qP9aB3Xr",
    "name": "Магазин 3 ИП Иванов",
    "isActive": false
  }
]
```

### Response Fields

| Field       | Type    | Description                                         | Required |
|-------------|---------|-----------------------------------------------------|----------|
| `id`        | string  | Уникальный ID магазина (Store ID)                   | ✅ Да    |
| `name`      | string  | Название магазина (для отображения пользователю)    | ✅ Да    |
| `isActive`  | boolean | Активен ли магазин (можно ли им пользоваться)       | ✅ Да    |

**Примечания:**
- `id` — это тот же `Store ID`, который используется в `/api/stores/{storeId}/complaints`
- `name` — это человеко-читаемое название магазина
- `isActive: false` — магазин будет показан в dropdown, но будет disabled/grey

### Error Responses

#### 401 Unauthorized
```json
{
  "error": "Invalid or expired token",
  "statusCode": 401
}
```

**Когда:** Неверный или отсутствующий `Authorization` header.

#### 429 Too Many Requests
```json
{
  "error": "Rate limit exceeded",
  "statusCode": 429,
  "retryAfter": 60
}
```

**Когда:** Превышен rate limit (100 запросов/минуту).

---

## 🔐 Security Considerations

### 1. Token-Based Access Control
- **Endpoint ДОЛЖЕН возвращать ТОЛЬКО магазины**, к которым у токена есть доступ
- Если токен привязан к одному магазину → возвращать массив с одним элементом
- Если токен мастер-токен → возвращать все магазины пользователя

### 2. Не Показывать Sensitive Data
- **НЕ возвращать:** API tokens, secrets, credentials
- **Только:** ID + название + статус

### 3. Rate Limiting
- Применить тот же rate limit: **100 requests/minute**
- Возвращать заголовки: `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`

---

## 💻 Frontend Implementation Plan

### Что изменится в расширении:

#### 1. Загрузка магазинов в `complaints-page.js`

**Было (старый API pilot-entry.ru):**
```javascript
async function loadStores() {
  const response = await fetch('https://pilot-entry.ru/api/stores', {
    headers: {
      Authorization: `Bearer ${OLD_PILOT_TOKEN}`
    }
  });
  const stores = await response.json();
  // ...
}
```

**Станет (новый backend):**
```javascript
async function loadStores() {
  const backendEndpoint = await settingsService.getBackendEndpoint();
  const backendToken = await settingsService.getBackendToken();

  const response = await fetch(`${backendEndpoint}/api/stores`, {
    headers: {
      Authorization: `Bearer ${backendToken}`,
      'Content-Type': 'application/json'
    }
  });

  if (!response.ok) {
    throw new Error(`Failed to load stores: HTTP ${response.status}`);
  }

  const stores = await response.json();

  // Populate dropdown
  storeSelect.innerHTML = '<option value="">Выберите магазин</option>';
  stores.forEach(store => {
    const option = document.createElement('option');
    option.value = store.id; // Store ID
    option.textContent = store.name; // Название магазина
    option.disabled = !store.isActive; // Неактивные магазины disabled
    storeSelect.appendChild(option);
  });
}
```

#### 2. Использование выбранного Store ID

**При подаче жалоб:**
```javascript
// Пользователь выбрал магазин из dropdown
const selectedStoreId = storeSelect.value; // "ss6Y8orHTX6vS7SgJl4k"

// Запрашиваем жалобы для выбранного магазина
const complaints = await pilotAPI.getComplaints(selectedStoreId);

// Пользователь НЕ ВИДИТ Store ID — только название магазина!
```

#### 3. UI/UX Изменения

**В `complaints-page.html`:**
```html
<div class="form-group">
  <label for="store-select">Выберите магазин:</label>
  <select id="store-select">
    <option value="">Загрузка магазинов...</option>
    <!-- Будет заполнено из /api/stores -->
  </select>
  <small style="color: #666; font-size: 12px;">
    💡 Выберите магазин, с которым хотите работать
  </small>
</div>
```

**Результат для пользователя:**
- Видит dropdown с понятными названиями: "20Grace ИП Ширазданова Г. М."
- НЕ видит технические Store ID
- Просто выбирает магазин и работает!

---

## 🧪 Testing Scenarios

### Test 1: Успешная загрузка магазинов
```bash
curl -X GET "http://158.160.217.236/api/stores" \
  -H "Authorization: Bearer d794d4408ef0955a693afaa913dd195ddd25f447e94d098c2f6e0155aa2b0038"
```

**Ожидается:**
```json
[
  {
    "id": "ss6Y8orHTX6vS7SgJl4k",
    "name": "20Grace ИП Ширазданова Г. М.",
    "isActive": true
  }
]
```

### Test 2: Неверный токен
```bash
curl -X GET "http://158.160.217.236/api/stores" \
  -H "Authorization: Bearer invalid_token_12345"
```

**Ожидается:** HTTP 401 Unauthorized

### Test 3: Rate Limit
- Отправить 101 запрос за минуту
- **Ожидается:** 101-й запрос вернет HTTP 429

### Test 4: Отсутствие Authorization Header
```bash
curl -X GET "http://158.160.217.236/api/stores"
```

**Ожидается:** HTTP 401 Unauthorized

---

## 📊 Business Value

### Для Пользователя:
- ✅ **Нет необходимости копировать/вставлять Store ID**
- ✅ **Быстрое переключение между магазинами** (один клик)
- ✅ **Понятный интерфейс** (названия магазинов, не технические ID)
- ✅ **Меньше ошибок** (нет риска ввести неправильный Store ID)

### Для Разработки:
- ✅ **Улучшенный UX** → больше пользователей используют расширение
- ✅ **Меньше тикетов в поддержку** (не нужно объяснять где взять Store ID)
- ✅ **Стандартный паттерн** (аналогично старому pilot-entry.ru API)

---

## 🔄 Compatibility

### Обратная Совместимость
- **Этот endpoint НЕ ЛОМАЕТ существующие API**
- Все текущие endpoints (`/api/stores/{storeId}/complaints`, `/api/stores/{storeId}/reviews/{reviewId}/complaint/sent`) работают БЕЗ ИЗМЕНЕНИЙ
- Это **дополнительный** endpoint для convenience

### Migration Path
Если endpoint будет добавлен:
1. Frontend обновит `complaints-page.js` для использования нового endpoint
2. Старый механизм (ручной ввод Store ID в настройках) останется как fallback
3. Если `/api/stores` недоступен → показываем сообщение: "Введите Store ID в настройках"

---

## ⏱️ Timeline Estimate (Frontend)

После добавления endpoint на backend, frontend реализация займет:

| Task                                  | Estimate |
|---------------------------------------|----------|
| Обновить `complaints-page.js`         | 2 часа   |
| Обновить UI в `complaints-page.html`  | 1 час    |
| Добавить error handling               | 1 час    |
| Тестирование на реальных данных       | 2 часа   |
| **TOTAL**                             | **6 часов** |

---

## 🙋 Questions for Backend Team

1. **Сколько времени потребуется на реализацию?**
   - Добавление endpoint
   - Тестирование
   - Deploy в production

2. **Есть ли ограничения на количество магазинов?**
   - Какой максимум магазинов может быть у одного пользователя?
   - Нужна ли пагинация для `/api/stores`?

3. **Как работает токен-доступ к магазинам?**
   - Один токен = один магазин?
   - Или существуют мастер-токены с доступом ко всем магазинам?

4. **Какие поля можно добавить в ответ?**
   - Может быть полезно: `createdAt`, `lastActivityAt`, `complaintsCount`
   - Но для MVP достаточно: `id`, `name`, `isActive`

---

## 📞 Contact

**Frontend Developer:**
- Работа через Claude Code
- Вопросы и обратная связь через Git Issues

**Документация:**
- Backend API Docs: [BACKEND_API_READY.md](../BACKEND_API_READY.md)
- Integration Plan: [PHASE_1_INTEGRATION_PLAN.md](./PHASE_1_INTEGRATION_PLAN.md)
- Testing Guide: [TESTING_INSTRUCTIONS.md](../TESTING_INSTRUCTIONS.md)

---

## ✅ Acceptance Criteria

Endpoint считается готовым, когда:

- ✅ `GET /api/stores` возвращает список магазинов с `id`, `name`, `isActive`
- ✅ Работает авторизация через `Bearer Token`
- ✅ Возвращает ТОЛЬКО магазины, к которым у токена есть доступ
- ✅ Применяется rate limiting (100 req/min)
- ✅ Возвращает правильные error codes (401, 429)
- ✅ Работает на production: `http://158.160.217.236`
- ✅ Frontend успешно загружает магазины в complaints-page.html

---

**Спасибо за рассмотрение! 🚀**

Этот endpoint значительно улучшит UX для пользователей с несколькими магазинами.
