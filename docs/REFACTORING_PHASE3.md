# Phase 3: Рефакторинг content.js и переход на идентификацию без ID

**Дата:** 28 января 2026
**Статус:** 🔄 В процессе
**Приоритет:** 🔥 Критический

---

## Executive Summary

**Проблема:** Wildberries изменил интерфейс в январе 2026:
- Убрал ID отзывов из DOM
- Добавил новую колонку "Инфо о товаре"
- Изменил структуру пагинации

**Текущее состояние:**
- ❌ `getIdFromRow()` возвращает `null` для всех отзывов
- ❌ Отзывы не находятся на странице (0 из N)
- ❌ Пагинация не работает (проверка по ID первой строки)
- ❌ content.js - монолитный файл 1601 строка

**Решение:**
1. ✅ Переход на поиск по комбинации: **Артикул + Рейтинг + Дата отзыва**
2. 🔄 Рефакторинг content.js на модули (как было с background.js)
3. ✅ Обновление документации

---

## 1. Изменения в идентификации отзывов

### 1.1 Новый механизм поиска

**Было (до 28.01.2026):**
```javascript
// Поиск по ID отзыва
const id = getIdFromRow(row);
if (targetIds.has(id)) {
  // Обрабатываем отзыв
}
```

**Стало (после 28.01.2026):**
```javascript
// Поиск по комбинации: артикул + рейтинг + дата
const reviewDate = getReviewDateFromRow(row);  // "18.01.2026"
const rating = getReviewRatingFromRow(row);     // 1-5
const productId = currentArticle;               // "187489568"

const key = `${productId}_${rating}_${reviewDate}`;
if (targetComplaintsMap.has(key)) {
  // Обрабатываем отзыв
}
```

### 1.2 Новые функции в utils.js

**Добавлено 28.01.2026:**

```javascript
/**
 * Получить дату отзыва из строки таблицы
 * Формат: "18.01.2026"
 */
function getReviewDateFromRow(row) {
  const dateContainer = row.querySelector('[class*="Col-date-time-with-readmark__date-with-marker"]');
  const dateSpan = dateContainer?.querySelector('span');
  return dateSpan?.textContent.trim() || null;
}

/**
 * Получить рейтинг отзыва из строки таблицы
 * Подсчитывает оранжевые звезды (fill="#FF773C")
 * Возвращает: 1-5
 */
function getReviewRatingFromRow(row) {
  const ratingContainer = row.querySelector('[class*="Rating__"]');
  const orangeStars = ratingContainer?.querySelectorAll('svg path[fill="#FF773C"]');
  return orangeStars?.length || null;
}
```

### 1.3 Изменения в API

**Требования к бэкенду:**
```json
{
  "id": "review_123",
  "productId": "187489568",
  "rating": 1,
  "reviewDate": "18.01.2026",  // ← НОВОЕ ПОЛЕ (обязательное)
  "complaintText": "..."
}
```

**Формат даты:** `DD.MM.YYYY` (например: "18.01.2026")

---

## 2. Code Review: content.js

### 2.1 Текущее состояние

**Размер:** 1601 строка
**Структура:** Монолитный файл с одним огромным message listener
**Проблемы:**
- ❌ Все функции вложены в `chrome.runtime.onMessage.addListener`
- ❌ Два полностью дублирующихся обработчика:
  - Legacy: `request.type === "searchParametrs"` (строки 171-650)
  - Optimized: `request.type === "processComplaintsFromAPI"` (строки 656-1590)
- ❌ Отсутствие модульности
- ❌ Сложно тестировать
- ❌ Сложно поддерживать

### 2.2 Структура файла

```
content.js (1601 строк)
├── Защита от повторного выполнения (строки 1-3)
├── Импорт из window.WBUtils (строки 5-23)
├── УНИВЕРСАЛЬНЫЕ функции поиска (строки 25-161)
│   ├── findMenuButtonLegacy()
│   ├── findOpenDropdownLegacy()
│   ├── findComplaintButtonLegacy()
│   ├── closeOpenDropdownLegacy()
│   └── findSubmitButtonLegacy()
│
└── chrome.runtime.onMessage.addListener (строки 163-1599)
    │
    ├── ===== LEGACY ОБРАБОТЧИК ===== (строки 171-650)
    │   ├── request.type === "searchParametrs"
    │   ├── Обработка по одному отзыву
    │   ├── Сканирование всех страниц
    │   └── Старый алгоритм (неэффективный)
    │
    └── ===== OPTIMIZED ОБРАБОТЧИК ===== (строки 656-1590)
        ├── request.type === "processComplaintsFromAPI"
        ├── Вспомогательные функции (строки 686-967)
        │   ├── sendProgress()
        │   ├── log()
        │   ├── groupComplaintsByArticle()
        │   ├── scanPageForReviews() ← НУЖНО ОБНОВИТЬ
        │   ├── goToNextPage() ← НУЖНО ОБНОВИТЬ
        │   ├── closeOpenDropdown()
        │   ├── submitComplaintForRow() (строки 1027-1354)
        │   └── processArticle() (строки 1357-1488) ← НУЖНО ОБНОВИТЬ
        │
        └── Основной цикл обработки (строки 1491-1590)
```

### 2.3 Критические проблемы

#### Проблема 1: Дублирование кода (80%)
**Legacy** и **Optimized** обработчики дублируют 80% логики:
- Поиск элементов (кнопки, dropdown, модалки)
- Заполнение форм
- Отправка жалоб
- Обработка ошибок

#### Проблема 2: Монолитная структура
Все 1601 строка в одном файле, все функции вложены в listener.

#### Проблема 3: Сложная поддержка
При изменении WB интерфейса нужно править код в 2-3 местах.

#### Проблема 4: Невозможность тестирования
Функции не экспортируются, тестировать нельзя.

---

## 3. План рефакторинга

### 3.1 Цели рефакторинга

1. ✅ **Модульность** - разделить на независимые модули
2. ✅ **Переиспользование** - убрать дублирование
3. ✅ **Тестируемость** - функции доступны для тестов
4. ✅ **Поддержка** - легко найти и изменить нужный код
5. ✅ **Масштабируемость** - легко добавлять новые функции

### 3.2 Новая структура файлов

```
src/contents/complaints/
├── content.js              # Входная точка (message listeners)
├── utils.js                # DOM утилиты ✅ УЖЕ ЕСТЬ
│
├── handlers/               # Обработчики запросов
│   ├── legacy-handler.js   # Legacy алгоритм (для обратной совместимости)
│   └── optimized-handler.js # Оптимизированный алгоритм
│
├── services/               # Бизнес-логика
│   ├── complaint-service.js    # Подача жалобы (основная логика)
│   ├── navigation-service.js   # Навигация по страницам
│   ├── search-service.js       # Поиск и фильтрация отзывов
│   └── progress-service.js     # Отслеживание прогресса
│
├── dom/                    # Работа с DOM
│   ├── element-finder.js   # Поиск элементов (кнопки, dropdown, модалки)
│   ├── form-filler.js      # Заполнение форм
│   └── data-extractor.js   # Извлечение данных из DOM (дата, рейтинг)
│
└── config/                 # Конфигурация
    └── selectors.js        # CSS селекторы WB
```

### 3.3 Приоритизация задач

#### Этап 1: Завершить текущие изменения (Сегодня, 28.01.2026)
**Статус:** 🔄 50% выполнено

- ✅ Добавить `getReviewDateFromRow()` в utils.js
- ✅ Добавить `getReviewRatingFromRow()` в utils.js
- ✅ Экспортировать функции в `window.WBUtils`
- ✅ Добавить `reviewDate` в complaints-page.js

**Осталось:**
- ⏳ Обновить `scanPageForReviews()` - поиск по ключу
- ⏳ Обновить `processArticle()` - использовать Map с ключами
- ⏳ Обновить `goToNextPage()` - проверка по дате первой строки

#### Этап 2: Тестирование новой логики (Сегодня)
- ⏳ Протестировать извлечение даты и рейтинга
- ⏳ Протестировать поиск отзывов по ключу
- ⏳ Протестировать пагинацию

#### Этап 3: Рефакторинг content.js (После тестирования)
- ⏳ Создать модульную структуру
- ⏳ Вынести функции в отдельные файлы
- ⏳ Убрать дублирование legacy/optimized
- ⏳ Написать unit тесты

#### Этап 4: Документация (Финал)
- ⏳ Обновить README.md
- ⏳ Создать CHANGELOG.md
- ⏳ Обновить USAGE_GUIDE.md

---

## 4. Детальный план модулей

### 4.1 Module: data-extractor.js

**Назначение:** Извлечение данных из строки таблицы

```javascript
/**
 * Извлечение данных из строки отзыва
 */
export class DataExtractor {
  /**
   * Получить дату отзыва (формат: "18.01.2026")
   */
  static getReviewDate(row) {
    const dateContainer = row.querySelector('[class*="Col-date-time-with-readmark__date-with-marker"]');
    const dateSpan = dateContainer?.querySelector('span');
    return dateSpan?.textContent.trim() || null;
  }

  /**
   * Получить рейтинг отзыва (1-5 звезд)
   */
  static getRating(row) {
    const ratingContainer = row.querySelector('[class*="Rating__"]');
    const orangeStars = ratingContainer?.querySelectorAll('svg path[fill="#FF773C"]');
    return orangeStars?.length || null;
  }

  /**
   * Создать уникальный ключ для отзыва
   * Формат: "productId_rating_reviewDate"
   */
  static createReviewKey(productId, rating, reviewDate) {
    return `${productId}_${rating}_${reviewDate}`;
  }

  /**
   * Получить ключ отзыва из строки таблицы
   */
  static getReviewKey(row, currentArticle) {
    const reviewDate = this.getReviewDate(row);
    const rating = this.getRating(row);

    if (!reviewDate || !rating) return null;

    return this.createReviewKey(currentArticle, rating, reviewDate);
  }
}
```

### 4.2 Module: element-finder.js

**Назначение:** Поиск элементов интерфейса WB

```javascript
/**
 * Поиск элементов в интерфейсе Wildberries
 */
export class ElementFinder {
  /**
   * Найти кнопку меню (три точки) в строке отзыва
   */
  static findMenuButton(row) {
    // Универсальная логика поиска
    const cells = Array.from(row.children);

    for (let i = cells.length - 1; i >= 0; i--) {
      const cell = cells[i];
      const buttonsCell = cell.querySelector('[class*="Buttons-cell"]');

      if (!buttonsCell) continue;

      const buttons = buttonsCell.querySelectorAll('button');
      for (const btn of buttons) {
        const svg = btn.querySelector('svg');
        if (svg) {
          const circles = svg.querySelectorAll('circle');
          if (circles.length >= 3) {
            return btn;
          }
        }
      }
    }

    return null;
  }

  /**
   * Найти открытый dropdown
   */
  static findOpenDropdown() {
    // Способ 1: По классу dropdown
    const dropdown = document.querySelector('[class*="dropdown"][class*="open"], [class*="Dropdown"][class*="open"]');
    if (dropdown) return dropdown;

    // Способ 2: По роли menu
    return document.querySelector('[role="menu"], [role="listbox"]');
  }

  /**
   * Найти кнопку "Пожаловаться" в dropdown
   */
  static findComplaintButton() {
    const dropdown = this.findOpenDropdown();
    if (!dropdown) return null;

    const buttons = dropdown.querySelectorAll('button, [role="menuitem"]');
    for (const btn of buttons) {
      const text = btn.textContent.toLowerCase();
      if (text.includes('пожаловаться') || text.includes('жалоб')) {
        return btn;
      }
    }

    return null;
  }

  /**
   * Найти кнопку отправки в модалке жалобы
   */
  static findSubmitButton(modal) {
    if (!modal) return null;

    const buttons = modal.querySelectorAll('button');
    for (const btn of buttons) {
      const text = btn.textContent.toLowerCase();
      if (text.includes('отправить') || text.includes('подать')) {
        return btn;
      }
    }

    return null;
  }
}
```

### 4.3 Module: search-service.js

**Назначение:** Поиск отзывов на странице

```javascript
import { DataExtractor } from '../dom/data-extractor.js';

/**
 * Сервис поиска отзывов на странице
 */
export class SearchService {
  /**
   * Сканировать страницу и найти нужные отзывы
   * @param {Map} complaintsMap - Map с жалобами (ключ: productId_rating_date)
   * @param {string} currentArticle - текущий артикул
   * @returns {Array} - найденные отзывы [{complaint, row}]
   */
  static scanPageForReviews(complaintsMap, currentArticle) {
    const found = [];
    const table = document.querySelector(".Base-table-body__F-y98zdE6m");

    if (!table || !table.children) {
      console.warn('[SearchService] Таблица не найдена');
      return found;
    }

    const rows = Array.from(table.children);
    console.log(`[SearchService] Всего строк на странице: ${rows.length}`);

    for (const row of rows) {
      if (!row) continue;

      // Получаем ключ отзыва
      const key = DataExtractor.getReviewKey(row, currentArticle);

      if (!key) {
        console.warn('[SearchService] Не удалось извлечь ключ из строки');
        continue;
      }

      // Ищем жалобу по ключу
      if (complaintsMap.has(key)) {
        const complaint = complaintsMap.get(key);
        found.push({ complaint, row });
        console.log(`✅ [SearchService] Найден отзыв: ${key}`);
      }
    }

    console.log(`[SearchService] Найдено: ${found.length} из ${complaintsMap.size}`);
    return found;
  }
}
```

### 4.4 Module: navigation-service.js

**Назначение:** Навигация по страницам (пагинация)

```javascript
import { DataExtractor } from '../dom/data-extractor.js';

/**
 * Сервис навигации по страницам
 */
export class NavigationService {
  /**
   * Перейти на следующую страницу
   * @returns {Promise<boolean>} - true если страница переключилась
   */
  static async goToNextPage() {
    const nextButton = window.WBUtils.findNextPageButton(true);

    if (!nextButton) {
      console.log('[NavigationService] ❌ Кнопка "Следующая страница" не найдена');
      return false;
    }

    if (!window.WBUtils.canGoToNextPage(nextButton)) {
      console.log('[NavigationService] ❌ Кнопка disabled - последняя страница');
      return false;
    }

    // Получаем дату первой строки ДО клика
    const tableBefore = document.querySelector(".Base-table-body__F-y98zdE6m");
    const firstRowDateBefore = tableBefore?.children[0]
      ? DataExtractor.getReviewDate(tableBefore.children[0])
      : null;

    console.log(`[NavigationService] ДО клика: дата первой строки = ${firstRowDateBefore}`);

    // Кликаем
    nextButton.click();
    console.log('[NavigationService] 🖱️ Клик выполнен, ждем загрузки...');

    await window.WBUtils.sleep(1500);

    // Проверяем дату ПОСЛЕ клика
    const tableAfter = document.querySelector(".Base-table-body__F-y98zdE6m");
    const firstRowDateAfter = tableAfter?.children[0]
      ? DataExtractor.getReviewDate(tableAfter.children[0])
      : null;

    console.log(`[NavigationService] ПОСЛЕ клика: дата первой строки = ${firstRowDateAfter}`);

    // Страница переключилась если дата изменилась
    const pageChanged = firstRowDateBefore !== firstRowDateAfter && firstRowDateAfter !== null;

    if (pageChanged) {
      console.log('[NavigationService] ✅ Страница успешно переключена');
      return true;
    } else {
      console.warn('[NavigationService] ⚠️ Страница НЕ переключилась');
      return false;
    }
  }
}
```

---

## 5. Текущий прогресс

### ✅ Выполнено (3/6 задач)

1. ✅ Добавлены функции `getReviewDateFromRow()` и `getReviewRatingFromRow()` в utils.js
2. ✅ Экспортированы новые функции в `window.WBUtils`
3. ✅ Добавлено поле `reviewDate` в complaints-page.js

### 🔄 В процессе (3/6 задач)

4. ⏳ Обновить `scanPageForReviews()` для поиска по ключу
5. ⏳ Обновить `processArticle()` для работы с Map
6. ⏳ Обновить `goToNextPage()` для проверки по дате

---

## 6. Следующие шаги

### Немедленно (Сегодня, 28.01.2026)

1. **Завершить изменения в content.js:**
   - Обновить `scanPageForReviews()` (строки 972-994)
   - Обновить `processArticle()` (строки 1357-1488)
   - Обновить `goToNextPage()` (строки 1000-1046)

2. **Тестирование:**
   - Запустить расширение
   - Проверить извлечение даты/рейтинга
   - Проверить поиск отзывов
   - Проверить пагинацию

3. **Если тесты успешны:**
   - Зафиксировать изменения
   - Обновить документацию
   - Перейти к рефакторингу

### Краткосрочно (1-2 дня)

4. **Рефакторинг content.js:**
   - Создать модульную структуру
   - Вынести функции в отдельные файлы
   - Убрать дублирование legacy/optimized

5. **Документация:**
   - Обновить README.md (версия 1.3.0)
   - Создать CHANGELOG.md
   - Обновить USAGE_GUIDE.md

---

## 7. Риски и митигация

### Риск 1: API не возвращает reviewDate
**Вероятность:** Средняя
**Влияние:** Критическое
**Митигация:** Согласовать с бэкенд командой добавление поля

### Риск 2: Формат даты не совпадает
**Вероятность:** Низкая
**Влияние:** Высокое
**Митигация:** Нормализация формата даты в коде

### Риск 3: WB снова изменит интерфейс
**Вероятность:** Высокая
**Влияние:** Высокое
**Митигация:** Модульная архитектура для быстрой адаптации

### Риск 4: Производительность с большим объемом
**Вероятность:** Средняя
**Влияние:** Среднее
**Митигация:** Оптимизация алгоритмов, кэширование

---

## 8. Метрики успеха

### Функциональные метрики
- ✅ Отзывы находятся на странице (>95%)
- ✅ Пагинация работает корректно
- ✅ Жалобы подаются успешно (>90%)

### Технические метрики
- ✅ Размер content.js: <500 строк (сейчас: 1601)
- ✅ Модульность: 80% кода в отдельных модулях
- ✅ Дублирование кода: <10% (сейчас: ~80%)
- ✅ Покрытие тестами: >60%

### Метрики качества
- ✅ ESLint: 0 ошибок
- ✅ Prettier: 100% форматирование
- ✅ Документация: 100% покрытие API

---

## 9. Заключение

Phase 3 - это критический этап, который решает две задачи:

1. **Адаптация к новому интерфейсу WB** (без ID отзывов)
2. **Рефакторинг монолитного content.js** на модули

После завершения Phase 3:
- ✅ Расширение будет работать с новым интерфейсом WB
- ✅ Код будет модульным и тестируемым
- ✅ Поддержка и развитие станут проще
- ✅ Готовность к Phase 4 (UI/UX улучшения)

**Приоритет:** 🔥 Критический
**Срок:** 28-29 января 2026
**Ответственный:** Development Team

---

**Версия документа:** 1.0
**Последнее обновление:** 28 января 2026
