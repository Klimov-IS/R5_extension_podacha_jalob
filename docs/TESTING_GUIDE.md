# 🧪 GUIDE ПО ТЕСТИРОВАНИЮ ИСПРАВЛЕНИЙ

## ✅ ЧТО БЫЛО ИСПРАВЛЕНО

### 🔥 КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ:
1. **Парсинг даты и времени** - дата и время теперь извлекаются из РАЗНЫХ элементов
2. **Логика пагинации** - кнопка "Следующая" теперь берется по ФИКСИРОВАННОМУ индексу [2]
3. **Парсинг рейтинга** - подсчет звезд улучшен (SVG вместо path)

---

## 📋 ПОШАГОВЫЙ ТЕСТ

### ШАГ 1: Перезагрузите расширение

1. Откройте `chrome://extensions/`
2. Найдите расширение **"Р5 подача жалоб"**
3. Нажмите кнопку **🔄 "Обновить"** (иконка обновления справа)
4. Проверьте что версия: **1.3.0**

---

### ШАГ 2: Откройте страницу отзывов WB

1. Перейдите на: `https://seller.wildberries.ru/feedbacks`
2. Убедитесь что видите таблицу с отзывами
3. Откройте **DevTools** (F12)
4. Перейдите на вкладку **Console**

---

### ШАГ 3: Запустите расширение

1. Кликните на иконку расширения "Р5 подача жалоб"
2. Нажмите кнопку, которая откроет **complaints-page.html**
3. Выберите **магазин** из списка
4. Выберите **звезды** (например, 1-2)
5. Нажмите **"Начать обработку"**

**НЕ нажимайте "Подтвердить" сразу!** Сначала проверим логи.

---

### ШАГ 4: Проверьте что скрипты загружены

В консоли DevTools на странице WB вы должны увидеть логи:

```
[WBUtils] Утилиты успешно загружены
[DataExtractor] Модуль успешно загружен
[ElementFinder] Модуль успешно загружен
[SearchService] Модуль успешно загружен
[NavigationService] Модуль успешно загружен
[ProgressService] Модуль успешно загружен
[ComplaintService] Модуль успешно загружен
[OptimizedHandler] Модуль успешно загружен
[Complaints] ✅ Все модули загружены успешно
[Complaints] ✅ Скрипт успешно инициализирован
```

**Если вы НЕ видите этих логов**, значит скрипты не загрузились. Попробуйте:
- Перезагрузить страницу WB (F5)
- Перезагрузить расширение
- Повторить Шаг 3

---

### ШАГ 5: Тестирование ВРУЧНУЮ (опционально)

Теперь вы можете протестировать функции вручную в консоли DevTools:

#### Тест 1: Парсинг даты и времени

```javascript
// Найти первую строку таблицы
const table = document.querySelector('.Base-table-body__F-y98zdE6m');
const row = table?.children[0];

if (!row) {
  console.error('❌ Таблица не найдена! Проверьте что вы на странице отзывов.');
} else {
  // Проверить парсинг даты
  const date = window.DataExtractor.getReviewDate(row);
  console.log('✅ Parsed datetime:', date);
  console.log('   Ожидается ISO 8601 формат: "2026-01-18T12:33:00.000Z"');

  // Проверить парсинг рейтинга
  const rating = window.DataExtractor.getRating(row);
  console.log('✅ Parsed rating:', rating);
  console.log('   Ожидается число от 1 до 5');
}
```

**Ожидаемый результат:**
```
✅ Parsed datetime: 2026-01-18T12:33:00.000Z
   Ожидается ISO 8601 формат: "2026-01-18T12:33:00.000Z"
✅ Parsed rating: 1
   Ожидается число от 1 до 5
```

#### Тест 2: Проверка кнопки пагинации

```javascript
const nextBtn = window.WBUtils.findNextPageButton(true);
console.log('Next button:', nextBtn);
console.log('Is disabled:', nextBtn?.disabled);
```

**Ожидаемый результат (на 1-й странице):**
```
[WBUtils] Ищем кнопку следующей страницы...
[WBUtils] Найдено 4 кнопок пагинации
[WBUtils] Кнопка [0] "В начало ◀◀": disabled=true
[WBUtils] Кнопка [1] "Предыдущая ◀": disabled=true
[WBUtils] Кнопка [2] "Следующая ▶": disabled=false  <-- ДОЛЖНА БЫТЬ false!
[WBUtils] Кнопка [3] "В конец ▶▶": disabled=false
[WBUtils] ✅ Кнопка "Следующая страница" [2] найдена и активна
Next button: <button...>
Is disabled: false
```

---

### ШАГ 6: Запустите обработку жалоб

1. Вернитесь в окно **complaints-page.html**
2. Нажмите **"Подтвердить запуск"**
3. Следите за логами в complaints-page и в консоли WB

---

## 🔍 ЧТО ПРОВЕРЯТЬ В ЛОГАХ

### В консоли WB (DevTools на странице feedbacks):

```
[DataExtractor] Extracted datetime text: 18.01.2026 в 12:33 {date: "18.01.2026", time: "12:33"}
[DataExtractor] Parsed ISO datetime: 2026-01-18T09:33:00.000Z
[DataExtractor] Extracted rating: 1 stars
```

**✅ ХОРОШО** - если видите строки выше
**❌ ПЛОХО** - если видите:
- `Date container not found in row`
- `Time span not found in parent container`
- `Failed to parse WB datetime`

### При переключении страниц:

```
[NavigationService] ДО клика: 100 строк, дата первой строки = 2026-01-18T09:33:00.000Z
[NavigationService] 🖱️ Клик выполнен, ждем загрузки...
[NavigationService] ПОСЛЕ клика: 100 строк, дата первой строки = 2026-01-17T14:20:00.000Z
[NavigationService] ✅ Страница успешно переключена
```

**✅ ХОРОШО** - если дата изменилась
**❌ ПЛОХО** - если дата НЕ изменилась (бесконечный цикл)

---

## 🚨 ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Проблема 1: "Cannot read properties of undefined (reading 'getReviewDate')"

**Причина:** Скрипты не инжектированы на страницу.

**Решение:**
1. Убедитесь что вы запустили расширение (Шаг 3)
2. Проверьте логи инжекта (Шаг 4)
3. Если логов нет - перезагрузите страницу WB (F5)

### Проблема 2: "Date container not found in row"

**Причина:** WB изменил структуру HTML (снова!).

**Решение:**
1. Скопируйте HTML первой строки таблицы:
   ```javascript
   const row = document.querySelector('.Base-table-body__F-y98zdE6m')?.children[0];
   console.log(row?.outerHTML);
   ```
2. Отправьте мне HTML - я обновлю селекторы

### Проблема 3: "Page not switched after click"

**Причина:** Кнопка пагинации не работает или изменилась.

**Решение:**
1. Проверьте структуру кнопок:
   ```javascript
   const buttons = document.querySelectorAll('[class*="Token-pagination__arrow"]');
   buttons.forEach((btn, i) => console.log(`[${i}] disabled=${btn.disabled}`));
   ```
2. Если структура изменилась - отправьте мне результат

---

## ✅ КРИТЕРИИ УСПЕХА

Тест считается **успешным**, если:

1. ✅ Скрипты загружаются без ошибок (Шаг 4)
2. ✅ Парсинг даты возвращает ISO 8601 формат (Шаг 5, Тест 1)
3. ✅ Парсинг рейтинга возвращает число 1-5 (Шаг 5, Тест 1)
4. ✅ Кнопка "Следующая" [2] найдена и активна (Шаг 5, Тест 2)
5. ✅ Жалобы успешно отправляются (Шаг 6)
6. ✅ Страницы переключаются корректно (проверка в логах)
7. ✅ Нет бесконечных циклов

---

## 📊 ОТЧЕТ О ТЕСТИРОВАНИИ

После тестирования отправьте мне:

1. **Скриншот логов** из консоли WB
2. **Скриншот логов** из complaints-page.html
3. **Результат**: сколько жалоб обработано / отправлено / ошибок

---

## 🆘 ПОМОЩЬ

Если что-то не работает:
1. Скопируйте **все логи** из консоли
2. Скопируйте **HTML первой строки** таблицы (см. Проблема 2)
3. Отправьте мне - я помогу!

---

**Удачи с тестированием! 🚀**
